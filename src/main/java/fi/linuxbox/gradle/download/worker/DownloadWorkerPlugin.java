package fi.linuxbox.gradle.download.worker;

import org.gradle.api.Plugin;
import org.gradle.api.Project;

public class DownloadWorkerPlugin implements Plugin<Project> {
    @Override
    public void apply(Project project) {
    }
}
